@model List<MCT.Web.Models.PlantModel>

<div id="searchResultContent">
    @*<div id="numberOfResults">
        Anzahl der gefundenen Species : @Model.Count
    </div>*@

    @*@foreach (var species in Model)*@
    <div id="patch-planer-search-result-content-rows" class="row">

        @foreach (var item in Model)
        {
        @Html.Partial("_plantPartialView", item);

        }
        
    </div>
</div>

<style type="text/css">
    #patch-planer-search-result-content-rows {

        height:450px;
        overflow:auto;
    }
</style>