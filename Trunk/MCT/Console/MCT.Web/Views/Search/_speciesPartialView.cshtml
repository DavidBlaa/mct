@{

    string headerClassName = "header";
    string btnClass = "btn btn-lg btn-block detailsBtn";

    if (Model.Type == SubjectType.Plant)
    {
        headerClassName += "plant";
        btnClass += "plant";
    }

    if (Model.Type == SubjectType.Animal)
    {
        headerClassName += "animal";
        btnClass += "animal";
    }

}

@model SpeciesModel
@using MCT.Web.Models;

<div id="@Model.Id" class="content @Model.Type.ToString().ToLower() col-lg-3 col-xs-12 col-sm-6 col-md-3" >
    <div class="@headerClassName" >
        <h3 class="name">
        @Html.ActionLink(@Model.Name, "Details", new { id = Model.Id, type = @Model.Type.ToString() })
        </h3>

    </div>
    <a class="fa fa-trash icon" onclick="deleteSubject(@Model.Id)" ></a>

    <h5 class="type">@Model.Type</h5>
    <h6 class="type">@Model.TaxonRank</h6>
    <h4 class="scientificName">@Model.ScientificName</h4>
    <img class=" img-responsive  speciesImage" src="@Model.ImagePath"/>
    
    @*<div class="details">
        <p class="description">@Model.Description</p>
    </div>*@
        @Html.ActionLink("Details", "Details", new { id = Model.Id, type = @Model.Type.ToString() }, new { @class = @btnClass })
</div>
