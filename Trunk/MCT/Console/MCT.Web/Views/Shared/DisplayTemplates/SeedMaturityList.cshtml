@model List<MCT.DB.Entities.SeedMaturity>
<dl class="dl-horizontal">
    @foreach (var item in Model)
    {

        <dd>
            @Html.DisplayFor(modelItem => item)
        </dd>
    }
</dl>