@model List<MCT.DB.Entities.TimePeriod>
<dl class="dl-horizontal">
    @foreach (var item in Model)
    {

        <dd>
            @Html.EditorFor(modelItem => item)
        </dd>
    }
</dl>